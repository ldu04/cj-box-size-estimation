# 제출 파일 — 2026-07-16 밤 (마지막 제출, 최종 확정)

과제제출 페이지에 이 폴더 안 2개 파일을 그대로 올리면 됩니다.

- `demo.zip` (68.9MB) — **detector_a + conf_thresh 0.4 + regressor(auto_match+순열불변손실) + 클램프**
  조합. 3차 제출과 동일한 detector 조합(리더보드 13.9262 확정 실측)에,
  이후 개선된 regressor는 그대로 유지.
- `result.json` — 50개 test 영상, 방금 demo.zip과 동일 코드로 Colab A100에서
  실제 `python main.py --input {test 폴더}` 실행해서 생성. 검증 완료(50개
  video_id 일치, 스키마 정상, 이상값 없음, 총 446개 박스).

## 오늘 있었던 일 요약 (다음에 참고)

1. 어젯밤 detector_b+클램프로 로컬 12.46까지 개선 확인해서 채택.
2. 오늘 아침 면적필터(`max_bbox_area_frac`)까지 추가해 로컬 12.412로 더
   개선 후 제출 → 리더보드 14.0042로 오히려 3차 제출(13.9262)보다 악화.
3. 처음엔 "면적필터가 원인"이라 판단해 필터만 롤백 시도했으나, 검증
   과정에서 실수(Colab에 옛 파일이 잘못 올라간 걸 못 알아챔)로 무효한
   테스트였음이 드러남 — 사용자가 이 실수를 지적해서 재검증.
4. 해시까지 검증하며 다시 테스트한 결과: **면적필터는 이 public test
   50개에서 단 한 번도 발동하지 않음**(있음/없음 예측이 완전히 동일,
   SHA256 일치). 즉 원인은 면적필터가 아니라 **detector_b 자체**.
5. detector_b는 로컬 100개 train 영상에서 추출한 프레임으로 파인튜닝된
   모델이라(`TRAIN_README.md`), 그 100개 영상엔 유리하지만 완전히
   새로운 영상(public test/hidden test)에는 오히려 detector_a보다
   못하다는 게 실측으로 확인됨.
6. 마지막 제출 1회를 안전하게 쓰기 위해 **detector_a로 최종 원복**.

**교훈**: 로컬 100개 train 검증만으로 채택 확정하면 안 됨 — 특히 그
100개로 학습/파인튜닝한 모델은 로컬 검증 자체가 편향돼 있을 수 있음.
제출 기회가 있으면 반드시 리더보드로 교차검증할 것.

상세 근거: `TEAM_MERGE_2026-07-16.md`, `HANDOFF_2026-07-15.md`(0-B 섹션),
git 커밋 로그(`c710673` 등) 참고
(`C:\Users\이동욱\Desktop\repo\CJ-Logistics-2026\`).
